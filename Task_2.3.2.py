# pomniejszanie list
lista_kierunku = ["północ", "południe", "wschód", "zachód"]

del lista_kierunku[0]
del lista_kierunku[0]
print(lista_kierunku)

lista_kierunku.remove("zachód")
print(lista_kierunku)
