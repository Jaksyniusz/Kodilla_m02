# powiększanie list
lista_kierunku = ["północ", "południe"]

lista_kierunku = lista_kierunku + ["zachód"]
print(lista_kierunku)

# lista_kierunku = lista_kierunku.append("wschód") nie działa - NoneType
lista_kierunku.append("wschód")
print(lista_kierunku)